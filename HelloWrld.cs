using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Hellowrld : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        //Output prn naší knozoli

        print("Hello world!");
        print(2024);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
